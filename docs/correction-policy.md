# Correction Policy

Published issues and PDFs are historical records and are never silently overwritten.

A correction increments `revision` and creates a new PDF suffix such as `_r2.pdf`. The manifest records date and reason. Canonical case records may also increment their own revision independently.

Stable IDs never change. Published folder paths never change. A corrected case does not retroactively modify an older issue snapshot.
